# MaroFlux PDE-ODE Hybrid Simulation — Python Dependencies
# ============================================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# For publication-grade Sobol analysis (Sec 3.4.2, N=2**13):
#   pip install SALib
#
# Python version tested: 3.10, 3.11, 3.12
# Author: Marwan Alaa Mohamed Mohamed Raslan
# Reference: MaroFlux_Final_v6.pdf

# ── Core numerical computation ────────────────────────────────────────────────
numpy>=1.24.0,<2.0.0          # Array operations, random sampling (Sec 3.4.3 Monte Carlo)
scipy>=1.11.0,<2.0.0          # solve_ivp (LSODA ODE), sparse solvers (PDE implicit)

# ── Visualization (publication-quality figures, 300 dpi) ─────────────────────
matplotlib>=3.7.0,<4.0.0      # 6-panel figure (panels A–F), bifurcation diagram

# ── Optional: Global Sensitivity Analysis (Sec 3.4.2) ────────────────────────
# Required for Sobol S1/ST indices via Saltelli sampling
# If absent, sensitivity section is gracefully skipped (HAS_SALIB = False)
# For N=2**13 publication run, install before executing:
SALib>=1.4.7,<2.0.0           # Saltelli sampling + Sobol analysis

# ── Standard library (no installation needed — included with Python) ──────────
# json       — JSON export of calibrated_params_pde_ode.json
# os         — Directory creation (figures/)
# warnings   — Suppression of scipy integration warnings
# time       — Simulation runtime measurement
